% 字符串输出
fprintf('笛卡尔坐标系中两点之间的距离是');
a = []; %空数组
[b,bint,r,rint,stats] = regress(Y,X)
% b是方程的系数矩阵，bint是回归系数的区间
% r残差,rint置信区间，stats用于检验回归模型是否正确，分别是R的平方，F值，概率P
% 随机产生1*10的[-7-15]的数字
randint(1,10,[-7?15])?